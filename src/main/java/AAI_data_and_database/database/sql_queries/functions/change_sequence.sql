---im pgadmin kann man die sequence folgendermassen ändern:

Select setval('contractlist_version_seq',120,false) ---start new sequence with primary key id = 120


---->tabelle anwählen, dann auf sql klicken. dann sieht man die sequence bzw den string den man einsetzen muss